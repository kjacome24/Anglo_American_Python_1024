let fruits = ['Apple', 'Banana', 'Cherry', 'Date'];

// Using forEach with access to both the element and its index
fruits.forEach((fruit, index) => {
    console.log(`Fruit at index ${index} is ${fruit}`);
});


fruits.forEach((fruit, index) => {
    console.log()
});